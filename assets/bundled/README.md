# assets/bundled

Cursor packs placed in this folder are treated as "built-in": they are always listed,
whatever cursors folder is selected in the app. The repository ships none, because cursor
packs belong to their artists and most of them forbid redistribution. If you fork this
project and have the rights to ship a pack, drop it here; it is ignored by git by default.
